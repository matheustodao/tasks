import styled from 'styled-components';

// Appling First Mobile
export const Container = styled.div`
  display: flex;
  flex-direction: column;
  flex-wrap: wrap;
  padding: 2.4rem 1.6rem;
  border-radius: 0;

`;
